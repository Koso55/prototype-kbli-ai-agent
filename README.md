# prototype-kbli-ai-agent

prototype for semantic search with kbli datas as the source of truth
Idea is simple, searches will return ranks, and need to be wrapped again into hunamized forms.

Scraped, fully, until 4th derivatives, and added embedding within fourth derivative docs.
Uses Atlas vector search, configured uses consine, with 384 dimensions due to llm's limitation.

TODO: need the wrapper functions, and an LLM as the conclusion maker and returh more humanized responses.
